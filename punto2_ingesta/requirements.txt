# Dependencias para la Lambda de ingesta y validación
# (boto3 viene incluido en runtime Lambda, no se empaqueta)
boto3>=1.34.0

# Solo para tests locales (no se empaqueta en Lambda)
moto[s3,cloudwatch]>=5.0.0
