# Dependencias base del proyecto (común a todos los módulos)
boto3>=1.34.0
botocore>=1.34.0
python-dotenv>=1.0.0

# Tests y calidad de código (compartido)
pytest>=7.4.0
pytest-cov>=4.1.0
flake8>=6.1.0
